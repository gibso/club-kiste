{% extends 'base.html.twig' %}

{% block body %}
    <h1>Eventseries</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eventseries.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eventseries_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('eventseries_edit', { 'id': eventseries.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
