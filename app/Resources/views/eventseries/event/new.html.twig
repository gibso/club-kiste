{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}

    <fieldset>
        <legend><h1>bearbeiten</h1></legend>

            <div class="form-group">
                {{ form_label(form.doorsopen) }}
                <div class="col-md-5">
                    {{ form_errors(form.doorsopen) }}
                    {{ form_widget(form.doorsopen) }}
                </div>
            </div>


        <div class="form-group">
            <label class="col-md-4 control-label" for="singlebutton"></label>
            <div class="col-md-4">
                <button id="singlebutton" name="singlebutton" type="submit"
                        class="btn btn-primary">Speichern
                </button>
            </div>
        </div>
    </fieldset>
    {{ form_end(form) }}

    <a class="btn" href="{{ path('eventseries_show', {'id': eventseries.id}) }}">Zurück</a>
    <br>
{% endblock %}