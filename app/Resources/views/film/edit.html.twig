{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(edit_form) }}

    <fieldset>
        <legend><h1>{{ modelName|capitalize }} bearbeiten</h1></legend>

            <div class="form-group">
                {{ form_label(edit_form.imdbId) }}
                <div class="col-md-5">
                    {{ form_errors(edit_form.imdbId) }}
                    {{ form_widget(edit_form.imdbId) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(edit_form.showtime) }}
                <div class="col-md-5">
                    {{ form_errors(edit_form.showtime) }}
                    {{ form_widget(edit_form.showtime) }}
                </div>
            </div>

        <div class="form-group">
            <label class="col-md-4 control-label" for="singlebutton"></label>
            <div class="col-md-4">
                <button id="singlebutton" name="singlebutton" type="submit"
                        class="btn btn-primary">Speichern
                </button>
            </div>
        </div>
    </fieldset>
    {{ form_end(edit_form) }}

    <a class="btn" href="{{ path(modelName ~ '_index') }}">Zurück</a>
    <br>
    {% if delete_form is defined %}
        {{ form_start(delete_form) }}
        <button id="singlebutton" name="singlebutton" type="submit"
                class="btn btn-danger">{{ modelName|capitalize }} löschen
        </button>
        {{ form_end(delete_form) }}
    {% endif %}
{% endblock %}
