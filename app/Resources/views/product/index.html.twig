{% extends 'model/index/layout.html.twig' %}

{% block beforeContent %}
    {% if alcoholic %}
        <script>
            if (!confirm("Na, bist du denn überhaupt schon 16 Jahre alt?")) {
                window.location.href = '/products';
            }
        </script>
    {% endif %}

    <div class="row">
        <div class="col-md-offset-4 col-md-2 alkoholfrei">
            <a href="{{ path('product_index', { 'alcoholic': false }) }}"><h4{% if not alcoholic %} style="font-weight: bold;" {% endif %}>Alkoholfrei</h4></a>
        </div>
        <div class="col-md-2 alkohol">
            <a href="{{ path('product_index', { 'alcoholic': true }) }}"><h4{% if alcoholic %} style="font-weight: bold;" {% endif %}>mit Alkohol</h4>
            </a>
        </div>
    </div>
{% endblock %}

{% block contentFooter %}
    <p>Quelle: {{ model.source }}</p>
{% endblock %}